<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.sabril.biomedis"
    android:versionCode="1"
    android:versionName="1.0" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk android:minSdkVersion="10" />
	<uses-permission android:name="android.permission.INTERNET" />
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" android:name="com.sabril.biomedis.Biomedis">
        <activity
            android:label="@string/app_name"
            android:name=".BiomedisActivity" android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="GraphActivity"></activity>
        <activity android:name="SettingsActivity"></activity>
        <activity android:name="AboutActivity"></activity>
    </application>

</manifest>